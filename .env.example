# ============================================================================
# UGS Metadata Server —— 环境变量示例
#
# 用法：复制成 .env 后按需修改（.env 已被 .gitignore 忽略）
#   cp .env.example .env
#
# 全部都有合理默认值，不配置也能直接 `deno task start`。
# 进程真实环境变量优先级高于本文件，因此 Docker / CI 里直接注入即可。
# ============================================================================

# ---------------------------------------------------------------------------
# 数据库
# ---------------------------------------------------------------------------
# SQLite 数据文件路径。相对路径基于进程工作目录；
# Docker 里建议用绝对路径（镜像内默认 /data/ugs.db）。
# 父目录不存在会自动创建，建表脚本会在首次运行时自动执行。
SQLITE_PATH=./data/ugs.db


# ---------------------------------------------------------------------------
# HTTP 服务
# ---------------------------------------------------------------------------
# 监听端口
PORT=8080

# 监听地址。Docker 里必须是 0.0.0.0；
# 只想本机访问（不暴露到局域网）就改成 127.0.0.1。
HOST=0.0.0.0
