# 与镜像无关的内容不要发给 Docker daemon，既能加快构建也能避免把本地数据/密钥打进镜像。
# 注意：deno.json / deno.lock / src / tools 是构建必需的，不能排除。

# 版本控制
.git
.gitignore
.gitattributes

# 本地环境变量（可能含自定义配置）
.env
.env.local
.env.*.local

# 本地 SQLite 数据（绝不要打进镜像）
data/
*.db
*.db-wal
*.db-shm

# 与运行时无关的目录
.github
docs
test
node_modules

# 文档与许可证（镜像里用不到）
*.md
LICENSE
